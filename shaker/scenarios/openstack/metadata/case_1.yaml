title: OpenStack instances metadata query

description:
  In this scenario Shaker launches instances that report their metadata back.
  Upon success the report should contain replies from all instances.

deployment:
  template: case_1.hot
  accommodation: [ single_room, density: 4 ]

execution:
  asynchronous: yes
  tests:
  -
    title: Get Instance Metadata
    class: kv
